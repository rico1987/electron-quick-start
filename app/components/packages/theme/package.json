{
  "name": "theme",
  "version": "1.0.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "scss": "cross-env ./node_modules/.bin/node-sass default/ -o lib/default/ --output-style compressed",
    "watch": "cross-env ./node_modules/.bin/node-sass --watch --recursive default/ -o lib/default/ --output-style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^5.1.6",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^5.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-cssmin": "^0.2.0",
    "gulp-sass": "^4.0.1",
    "node-sass": "^4.9.0"
  }
}
